# ---- Auth Service (.env.example) ----
# Puerto (Railway inyecta PORT automáticamente)
PORT=4000

# CORS: lista de orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:3000,https://tu-frontend.app

# Supabase
SUPABASE_URL=
SUPABASE_API_KEY=

# JWT
JWT_SECRET=pon-una-clave-segura

# (Opcional) URL base del frontend para construir redirecciones
FRONTEND_URL=https://tu-frontend.app
